{
  "name": "meta-lib",
  "version": "1.0.0",
  "description": "Meta (Facebook) Auth & Posting SDK",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --coverage --forceExit",
    "test:watch": "jest --watch"
  },
  "keywords": ["meta", "facebook", "auth", "api"],
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
